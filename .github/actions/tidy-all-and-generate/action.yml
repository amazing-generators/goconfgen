name: 'Normalize Go modules'
description: 'Prepare workspace and normalize Go module state'

runs:
  using: 'composite'
  steps:
    - name: Prepare workspace
      shell: bash
      run: |
        set -Eeuo pipefail
        if [[ -f go.work ]]; then
          go work sync
        fi
        go mod tidy
