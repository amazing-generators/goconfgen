server:
  host: 0.0.0.0
  port: 8443
  secure: true

logging:
  level: info
  outputs: [ console, json ]

limits:
  timeout: 45s
  body_max: 16MiB

labels:
  common:
    env: stage
    team: platform
