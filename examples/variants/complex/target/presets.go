// Code generated by goconfgen; DO NOT EDIT.
// Source schema: examples/source/config.yml

package complexcfg

import "sort"

// // // // // // // // // //
var cFullYAMLBytes = []byte(`# HTTP server settings.
# This branch demonstrates generated interfaces.
server:
  host: 127.0.0.1 # Listen host for inbound HTTP requests.
  port: 8080 # Listen port for inbound HTTP requests.
  secure: false # Enable TLS listener mode.
# Logging settings.
logging:
  level: info # Minimum log level.
  # Ordered logging sinks.
  outputs: [console]
# Resource protection settings.
limits:
  timeout: 15s # Request timeout.
  body_max: 8MiB # Maximum request body size.
# Feature toggles and rollout lists.
features:
  # Enabled feature keys.
  enabled: [metrics, tracing]
  # Per-environment rollout list.
  rollout:
    prod: [metrics]
    stage: [metrics, tracing]
# Static metadata labels.
labels:
  # Common labels included in every event.
  common:
    env: dev
    team: platform
# Arbitrary extension payload.
extensions:
  # Extra unstructured payload.
  raw:
    nested:
      retries: 3
      mode: safe
# Scalar numeric type coverage.
# Exercises every supported integer, float, duration, and size width.
numbers:
  i8: -8 # Signed 8-bit value.
  i16: 1616 # Signed 16-bit value.
  i32: 323232 # Signed 32-bit value.
  i64: 6464646464 # Signed 64-bit value.
  u: 7 # Unsigned word value.
  u8: 200 # Unsigned 8-bit value.
  u16: 60000 # Unsigned 16-bit value.
  u32: 4294967295 # Unsigned 32-bit value.
  u64: 64 # Unsigned 64-bit value.
  f: 1.5 # 32-bit float alias.
  f32: 2.5 # 32-bit float.
  f64: 3.5 # 64-bit float.
  dur_alias: 90s # Duration via Go alias type.
# Array type coverage.
# One field per supported array element type.
arrays:
  # Bool array.
  bools: [true, false, true]
  # Int array.
  ints: [1, 2, 3]
  # Int8 array.
  i8s: [-1, 0, 1]
  # Int16 array.
  i16s: [100, 200]
  # Int32 array.
  i32s: [1000]
  # Int64 array.
  i64s: [10000000000]
  # Uint array.
  us: [1, 2]
  # Uint8 array.
  u8s: [255]
  # Uint16 array.
  u16s: [65535]
  # Uint32 array.
  u32s: [4294967295]
  # Uint64 array.
  u64s: [64]
  # Float32 array.
  f32s: [1.1, 2.2]
  # Float64 array.
  f64s: [3.3]
  # Duration array.
  durations: [1s, 2s]
  # Duration alias array.
  dur_aliases: [5s]
  # Size array.
  sizes: [1KiB, 2MiB]
`)
var cFullJSONBytes = []byte(`{
  "server": {
    "host": "127.0.0.1",
    "port": 8080,
    "secure": false
  },
  "logging": {
    "level": "info",
    "outputs": ["console"]
  },
  "limits": {
    "timeout": "15s",
    "body_max": "8MiB"
  },
  "features": {
    "enabled": ["metrics","tracing"],
    "rollout": {
      "prod": ["metrics"],
      "stage": ["metrics","tracing"]
    }
  },
  "labels": {
    "common": {
      "env": "dev",
      "team": "platform"
    }
  },
  "extensions": {
    "raw": {
      "nested": {
        "retries": 3,
        "mode": "safe"
      }
    }
  },
  "numbers": {
    "i8": -8,
    "i16": 1616,
    "i32": 323232,
    "i64": 6464646464,
    "u": 7,
    "u8": 200,
    "u16": 60000,
    "u32": 4294967295,
    "u64": 64,
    "f": 1.5,
    "f32": 2.5,
    "f64": 3.5,
    "dur_alias": "90s"
  },
  "arrays": {
    "bools": [true,false,true],
    "ints": [1,2,3],
    "i8s": [-1,0,1],
    "i16s": [100,200],
    "i32s": [1000],
    "i64s": [10000000000],
    "us": [1,2],
    "u8s": [255],
    "u16s": [65535],
    "u32s": [4294967295],
    "u64s": [64],
    "f32s": [1.1,2.2],
    "f64s": [3.3],
    "durations": ["1s","2s"],
    "dur_aliases": ["5s"],
    "sizes": ["1KiB","2MiB"]
  }
}
`)
var cFullHJSONBytes = []byte(`{
  # HTTP server settings.
  # This branch demonstrates generated interfaces.
  server: {
    # Listen host for inbound HTTP requests.
    host: 127.0.0.1
    # Listen port for inbound HTTP requests.
    port: 8080
    # Enable TLS listener mode.
    secure: false
  }
  # Logging settings.
  logging: {
    # Minimum log level.
    level: info
    # Ordered logging sinks.
    outputs: [
      console
    ]
  }
  # Resource protection settings.
  limits: {
    # Request timeout.
    timeout: 15s
    # Maximum request body size.
    body_max: 8MiB
  }
  # Feature toggles and rollout lists.
  features: {
    # Enabled feature keys.
    enabled: [
      metrics
      tracing
    ]
    # Per-environment rollout list.
    rollout: {
      prod: [
        metrics
      ]
      stage: [
        metrics
        tracing
      ]
    }
  }
  # Static metadata labels.
  labels: {
    # Common labels included in every event.
    common: {
      env: dev
      team: platform
    }
  }
  # Arbitrary extension payload.
  extensions: {
    # Extra unstructured payload.
    raw: {
      nested: {
        retries: 3
        mode: safe
      }
    }
  }
  # Scalar numeric type coverage.
  # Exercises every supported integer, float, duration, and size width.
  numbers: {
    # Signed 8-bit value.
    i8: -8
    # Signed 16-bit value.
    i16: 1616
    # Signed 32-bit value.
    i32: 323232
    # Signed 64-bit value.
    i64: 6464646464
    # Unsigned word value.
    u: 7
    # Unsigned 8-bit value.
    u8: 200
    # Unsigned 16-bit value.
    u16: 60000
    # Unsigned 32-bit value.
    u32: 4294967295
    # Unsigned 64-bit value.
    u64: 64
    # 32-bit float alias.
    f: 1.5
    # 32-bit float.
    f32: 2.5
    # 64-bit float.
    f64: 3.5
    # Duration via Go alias type.
    dur_alias: 90s
  }
  # Array type coverage.
  # One field per supported array element type.
  arrays: {
    # Bool array.
    bools: [
      true
      false
      true
    ]
    # Int array.
    ints: [
      1
      2
      3
    ]
    # Int8 array.
    i8s: [
      -1
      0
      1
    ]
    # Int16 array.
    i16s: [
      100
      200
    ]
    # Int32 array.
    i32s: [
      1000
    ]
    # Int64 array.
    i64s: [
      10000000000
    ]
    # Uint array.
    us: [
      1
      2
    ]
    # Uint8 array.
    u8s: [
      255
    ]
    # Uint16 array.
    u16s: [
      65535
    ]
    # Uint32 array.
    u32s: [
      4294967295
    ]
    # Uint64 array.
    u64s: [
      64
    ]
    # Float32 array.
    f32s: [
      1.1
      2.2
    ]
    # Float64 array.
    f64s: [
      3.3
    ]
    # Duration array.
    durations: [
      1s
      2s
    ]
    # Duration alias array.
    dur_aliases: [
      5s
    ]
    # Size array.
    sizes: [
      1KiB
      2MiB
    ]
  }
}
`)

func FullConfig() *ConfigObj {
	obj, err := parsePrimaryBytes(cFullPrimaryBytes(), false)
	if err != nil {
		panic(err)
	}
	return obj
}

func FullYAML() []byte { return append([]byte(nil), cFullYAMLBytes...) }

func FullJSON() []byte { return append([]byte(nil), cFullJSONBytes...) }

func FullHJSON() []byte { return append([]byte(nil), cFullHJSONBytes...) }

func HasFull() bool { return true }

func cFullPrimaryBytes() []byte {
	return cFullYAMLBytes
}

var cMinimalYAMLBytes = []byte(`# HTTP server settings.
# This branch demonstrates generated interfaces.
server:
  host: 0.0.0.0 # Listen host for inbound HTTP requests.
  port: 8080 # Listen port for inbound HTTP requests.
# Logging settings.
logging:
  level: warn # Minimum log level.
# Resource protection settings.
limits:
  timeout: 30s # Request timeout.
`)
var cMinimalJSONBytes = []byte(`{
  "server": {
    "host": "0.0.0.0",
    "port": 8080
  },
  "logging": {
    "level": "warn"
  },
  "limits": {
    "timeout": "30s"
  }
}
`)
var cMinimalHJSONBytes = []byte(`{
  # HTTP server settings.
  # This branch demonstrates generated interfaces.
  server: {
    # Listen host for inbound HTTP requests.
    host: 0.0.0.0
    # Listen port for inbound HTTP requests.
    port: 8080
  }
  # Logging settings.
  logging: {
    # Minimum log level.
    level: warn
  }
  # Resource protection settings.
  limits: {
    # Request timeout.
    timeout: 30s
  }
}
`)

func MinimalConfig() *ConfigObj {
	obj, err := parsePrimaryBytes(cMinimalPrimaryBytes(), false)
	if err != nil {
		panic(err)
	}
	return obj
}

func MinimalYAML() []byte { return append([]byte(nil), cMinimalYAMLBytes...) }

func MinimalJSON() []byte { return append([]byte(nil), cMinimalJSONBytes...) }

func MinimalHJSON() []byte { return append([]byte(nil), cMinimalHJSONBytes...) }

func HasMinimal() bool { return true }

func cMinimalPrimaryBytes() []byte {
	return cMinimalYAMLBytes
}

var cMediumYAMLBytes = []byte(`# HTTP server settings.
# This branch demonstrates generated interfaces.
server:
  host: 0.0.0.0 # Listen host for inbound HTTP requests.
  port: 8443 # Listen port for inbound HTTP requests.
  secure: true # Enable TLS listener mode.
# Logging settings.
logging:
  level: info # Minimum log level.
  # Ordered logging sinks.
  outputs: [console, json]
# Resource protection settings.
limits:
  timeout: 45s # Request timeout.
  body_max: 16MiB # Maximum request body size.
# Static metadata labels.
labels:
  # Common labels included in every event.
  common:
    env: stage
    team: platform
`)
var cMediumJSONBytes = []byte(`{
  "server": {
    "host": "0.0.0.0",
    "port": 8443,
    "secure": true
  },
  "logging": {
    "level": "info",
    "outputs": ["console","json"]
  },
  "limits": {
    "timeout": "45s",
    "body_max": "16MiB"
  },
  "labels": {
    "common": {
      "env": "stage",
      "team": "platform"
    }
  }
}
`)
var cMediumHJSONBytes = []byte(`{
  # HTTP server settings.
  # This branch demonstrates generated interfaces.
  server: {
    # Listen host for inbound HTTP requests.
    host: 0.0.0.0
    # Listen port for inbound HTTP requests.
    port: 8443
    # Enable TLS listener mode.
    secure: true
  }
  # Logging settings.
  logging: {
    # Minimum log level.
    level: info
    # Ordered logging sinks.
    outputs: [
      console
      json
    ]
  }
  # Resource protection settings.
  limits: {
    # Request timeout.
    timeout: 45s
    # Maximum request body size.
    body_max: 16MiB
  }
  # Static metadata labels.
  labels: {
    # Common labels included in every event.
    common: {
      env: stage
      team: platform
    }
  }
}
`)

func MediumConfig() *ConfigObj {
	obj, err := parsePrimaryBytes(cMediumPrimaryBytes(), false)
	if err != nil {
		panic(err)
	}
	return obj
}

func MediumYAML() []byte { return append([]byte(nil), cMediumYAMLBytes...) }

func MediumJSON() []byte { return append([]byte(nil), cMediumJSONBytes...) }

func MediumHJSON() []byte { return append([]byte(nil), cMediumHJSONBytes...) }

func HasMedium() bool { return true }

func cMediumPrimaryBytes() []byte {
	return cMediumYAMLBytes
}

func PresetNames() []string {
	nameTextArr := make([]string, 0, len(cPresetConfigByNameMap))
	for nameText := range cPresetConfigByNameMap {
		nameTextArr = append(nameTextArr, nameText)
	}
	sort.Strings(nameTextArr)
	return nameTextArr
}

func Preset(nameText string) (*ConfigObj, bool) {
	ctorObj := cPresetConfigByNameMap[nameText]
	if ctorObj == nil {
		return nil, false
	}
	return ctorObj(), true
}

func PresetYAML(nameText string) ([]byte, bool) {
	dataArr, existsFlag := cPresetYAMLByNameMap[nameText]
	if !existsFlag {
		return nil, false
	}
	return append([]byte(nil), dataArr...), true
}

func PresetJSON(nameText string) ([]byte, bool) {
	dataArr, existsFlag := cPresetJSONByNameMap[nameText]
	if !existsFlag {
		return nil, false
	}
	return append([]byte(nil), dataArr...), true
}

func PresetHJSON(nameText string) ([]byte, bool) {
	dataArr, existsFlag := cPresetHJSONByNameMap[nameText]
	if !existsFlag {
		return nil, false
	}
	return append([]byte(nil), dataArr...), true
}

var cPresetConfigByNameMap = map[string]func() *ConfigObj{
	"full":    FullConfig,
	"minimal": MinimalConfig,
	"medium":  MediumConfig,
}

var cPresetYAMLByNameMap = map[string][]byte{
	"full":    cFullYAMLBytes,
	"minimal": cMinimalYAMLBytes,
	"medium":  cMediumYAMLBytes,
}

var cPresetJSONByNameMap = map[string][]byte{
	"full":    cFullJSONBytes,
	"minimal": cMinimalJSONBytes,
	"medium":  cMediumJSONBytes,
}

var cPresetHJSONByNameMap = map[string][]byte{
	"full":    cFullHJSONBytes,
	"minimal": cMinimalHJSONBytes,
	"medium":  cMediumHJSONBytes,
}
