// Code generated by goconfgen; DO NOT EDIT.
// Source schema: examples/source/config.yml

package complexcfg

import (
	"bytes"
	"encoding/json"
	"strconv"
)

// // // // // // // // // //

func (obj *ConfigObj) RenderJSON(partialFlag bool) ([]byte, error) {
	var bufferObj bytes.Buffer
	includeFlag, err := appendJSONConfigObj(&bufferObj, obj, partialFlag, "", true)
	if err != nil {
		return nil, err
	}
	if !includeFlag {
		return []byte("{}\n"), nil
	}
	return bufferObj.Bytes(), nil
}

func appendJSONConfigObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONServerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "server")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONLoggingObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "logging")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONLimitsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "limits")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONFeaturesObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "features")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONLabelsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "labels")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONExtensionsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "extensions")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONNumbersObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "numbers")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendJSONArraysObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "arrays")
			bodyObj.Write(childBufferObj.Bytes())
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONServerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("server.host") {
			valueObj, err := renderValueServerHost(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "host")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("server.port") {
			valueObj, err := renderValueServerPort(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "port")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("server.secure") {
			valueObj, err := renderValueServerSecure(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "secure")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONLoggingObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("logging.level") {
			valueObj, err := renderValueLoggingLevel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "level")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.outputs") {
			valueObj, err := renderValueLoggingOutputs(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "outputs")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONLimitsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("limits.timeout") {
			valueObj, err := renderValueLimitsTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "timeout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("limits.body_max") {
			valueObj, err := renderValueLimitsBodyMax(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "body_max")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONFeaturesObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("features.enabled") {
			valueObj, err := renderValueFeaturesEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "enabled")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("features.rollout") {
			valueObj, err := renderValueFeaturesRollout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "rollout")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONLabelsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("labels.common") {
			valueObj, err := renderValueLabelsCommon(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "common")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONExtensionsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("extensions.raw") {
			valueObj, err := renderValueExtensionsRaw(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "raw")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONNumbersObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("numbers.i8") {
			valueObj, err := renderValueNumbersI8(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "i8")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.i16") {
			valueObj, err := renderValueNumbersI16(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "i16")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.i32") {
			valueObj, err := renderValueNumbersI32(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "i32")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.i64") {
			valueObj, err := renderValueNumbersI64(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "i64")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.u") {
			valueObj, err := renderValueNumbersU(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.u8") {
			valueObj, err := renderValueNumbersU8(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u8")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.u16") {
			valueObj, err := renderValueNumbersU16(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u16")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.u32") {
			valueObj, err := renderValueNumbersU32(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u32")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.u64") {
			valueObj, err := renderValueNumbersU64(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u64")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.f") {
			valueObj, err := renderValueNumbersF(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "f")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.f32") {
			valueObj, err := renderValueNumbersF32(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "f32")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.f64") {
			valueObj, err := renderValueNumbersF64(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "f64")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("numbers.dur_alias") {
			valueObj, err := renderValueNumbersDurAlias(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "dur_alias")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONArraysObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	firstFlag := true
	{
		if !partialFlag || obj.hasPath("arrays.bools") {
			valueObj, err := renderValueArraysBools(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "bools")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.ints") {
			valueObj, err := renderValueArraysInts(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "ints")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.i8s") {
			valueObj, err := renderValueArraysI8S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "i8s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.i16s") {
			valueObj, err := renderValueArraysI16S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "i16s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.i32s") {
			valueObj, err := renderValueArraysI32S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "i32s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.i64s") {
			valueObj, err := renderValueArraysI64S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "i64s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.us") {
			valueObj, err := renderValueArraysUs(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "us")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.u8s") {
			valueObj, err := renderValueArraysU8S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u8s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.u16s") {
			valueObj, err := renderValueArraysU16S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u16s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.u32s") {
			valueObj, err := renderValueArraysU32S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u32s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.u64s") {
			valueObj, err := renderValueArraysU64S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "u64s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.f32s") {
			valueObj, err := renderValueArraysF32S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "f32s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.f64s") {
			valueObj, err := renderValueArraysF64S(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "f64s")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.durations") {
			valueObj, err := renderValueArraysDurations(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "durations")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.dur_aliases") {
			valueObj, err := renderValueArraysDurAliases(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "dur_aliases")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("arrays.sizes") {
			valueObj, err := renderValueArraysSizes(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := json.Marshal(valueObj)
			if err != nil {
				return false, err
			}
			appendJSONEntryPrefix(&bodyObj, &firstFlag, indentText, "sizes")
			bodyObj.Write(valueArr)
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString("\n")
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendJSONEntryPrefix(bufferObj *bytes.Buffer, firstFlag *bool, indentText string, keyText string) {
	if !*firstFlag {
		bufferObj.WriteString(",\n")
	}
	*firstFlag = false
	bufferObj.WriteString(indentText)
	bufferObj.WriteString("  ")
	bufferObj.WriteString(strconv.Quote(keyText))
	bufferObj.WriteString(": ")
}
