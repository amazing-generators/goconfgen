// Code generated by goconfgen; DO NOT EDIT.
// Source schema: examples/source/config.yml

package hjsoncfg

import (
	"bytes"
	"encoding/json"
	"strings"

	"github.com/hjson/hjson-go/v4"
)

// // // // // // // // // //

func (obj *ConfigObj) RenderHJSON(partialFlag bool) ([]byte, error) {
	var bufferObj bytes.Buffer
	includeFlag, err := appendHJSONConfigObj(&bufferObj, obj, partialFlag, "", true)
	if err != nil {
		return nil, err
	}
	if !includeFlag {
		return []byte("{}\n"), nil
	}
	return bufferObj.Bytes(), nil
}

func appendHJSONConfigObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONServerObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "server", "HTTP server settings.\nThis branch demonstrates generated interfaces.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONLoggingObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "logging", "Logging settings.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONLimitsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "limits", "Resource protection settings.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONFeaturesObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "features", "Feature toggles and rollout lists.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONLabelsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "labels", "Static metadata labels.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		var childBufferObj bytes.Buffer
		childIncludeFlag, err := appendHJSONExtensionsObj(&childBufferObj, obj, partialFlag, indentText+"  ", false)
		if err != nil {
			return false, err
		}
		if childIncludeFlag || !partialFlag {
			appendHJSONEntryPrefix(&bodyObj, indentText, "extensions", "Arbitrary extension payload.")
			bodyObj.Write(childBufferObj.Bytes())
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONServerObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("server.host") {
			valueObj, err := renderValueServerHost(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "host", "Listen host for inbound HTTP requests.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("server.port") {
			valueObj, err := renderValueServerPort(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "port", "Listen port for inbound HTTP requests.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("server.secure") {
			valueObj, err := renderValueServerSecure(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "secure", "Enable TLS listener mode.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONLoggingObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("logging.level") {
			valueObj, err := renderValueLoggingLevel(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "level", "Minimum log level.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("logging.outputs") {
			valueObj, err := renderValueLoggingOutputs(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "outputs", "Ordered logging sinks.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONLimitsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("limits.timeout") {
			valueObj, err := renderValueLimitsTimeout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "timeout", "Request timeout.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("limits.body_max") {
			valueObj, err := renderValueLimitsBodyMax(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "body_max", "Maximum request body size.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONFeaturesObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("features.enabled") {
			valueObj, err := renderValueFeaturesEnabled(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "enabled", "Enabled feature keys.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	{
		if !partialFlag || obj.hasPath("features.rollout") {
			valueObj, err := renderValueFeaturesRollout(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "rollout", "Per-environment rollout list.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONLabelsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("labels.common") {
			valueObj, err := renderValueLabelsCommon(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "common", "Common labels included in every event.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONExtensionsObj(bufferObj *bytes.Buffer, obj *ConfigObj, partialFlag bool, indentText string, rootFlag bool) (bool, error) {
	var bodyObj bytes.Buffer
	includeFlag := false
	{
		if !partialFlag || obj.hasPath("extensions.raw") {
			valueObj, err := renderValueExtensionsRaw(obj)
			if err != nil {
				return false, err
			}
			valueArr, err := renderHJSONValue(valueObj, indentText+"  ")
			if err != nil {
				return false, err
			}
			appendHJSONEntryPrefix(&bodyObj, indentText, "raw", "Extra unstructured payload.")
			bodyObj.Write(valueArr)
			bodyObj.WriteString("\n")
			includeFlag = true
		}
	}
	if !includeFlag && partialFlag {
		return false, nil
	}
	bufferObj.WriteString("{")
	if bodyObj.Len() > 0 {
		bufferObj.WriteString("\n")
		bufferObj.Write(bodyObj.Bytes())
		bufferObj.WriteString(indentText)
	}
	bufferObj.WriteString("}")
	if rootFlag {
		bufferObj.WriteString("\n")
	}
	return true, nil
}

func appendHJSONEntryPrefix(bufferObj *bytes.Buffer, indentText string, keyText string, usageText string) {
	if usageText != "" {
		for _, lineText := range strings.Split(usageText, "\n") {
			bufferObj.WriteString(indentText + "  # " + lineText + "\n")
		}
	}
	keyArr, err := renderHJSONKeyText(keyText)
	if err != nil {
		keyArr = []byte(jsonQuoteForHJSON(keyText))
	}
	bufferObj.WriteString(indentText)
	bufferObj.WriteString("  ")
	bufferObj.Write(keyArr)
	bufferObj.WriteString(": ")
}

func renderHJSONValue(valueObj any, indentText string) ([]byte, error) {
	optsObj := hjson.DefaultOptions()
	optsObj.Comments = false
	optsObj.EmitRootBraces = false
	optsObj.IndentBy = "  "
	optsObj.BaseIndentation = indentText
	return hjson.MarshalWithOptions(valueObj, optsObj)
}

func renderHJSONKeyText(keyText string) ([]byte, error) {
	if isBareHJSONKey(keyText) {
		return []byte(keyText), nil
	}
	return json.Marshal(keyText)
}

func isBareHJSONKey(keyText string) bool {
	if keyText == "" {
		return false
	}
	for itemIndex, itemRune := range keyText {
		switch {
		case itemRune >= 'a' && itemRune <= 'z':
		case itemRune >= 'A' && itemRune <= 'Z':
		case itemRune >= '0' && itemRune <= '9' && itemIndex > 0:
		case itemRune == '_' || itemRune == '-':
		default:
			return false
		}
	}
	return true
}

func jsonQuoteForHJSON(keyText string) string {
	dataArr, err := json.Marshal(keyText)
	if err != nil {
		return "\"\""
	}
	return string(dataArr)
}
